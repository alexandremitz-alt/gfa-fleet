[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "gfa-fleet-backend"
version = "1.0.0"
description = "GFA Fleet Control Backend API"
requires-python = ">=3.9"
